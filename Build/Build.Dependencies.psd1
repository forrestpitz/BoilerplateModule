@{
    # Testing the code
    Pester        = 'latest'

    # Writing MAML and Markdown external help
    PlatyPS       = 'latest'

    # Combine all the ps1 files into a single psm1 file 
    # To help module loading perf and make things like classes that dont span files well work
    ModuleBuilder = 'latest'
}